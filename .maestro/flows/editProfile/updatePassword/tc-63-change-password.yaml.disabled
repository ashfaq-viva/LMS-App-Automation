# Disabled by approval: changing the shared User 1 password is unsafe for unattended runs.
# appId: com.lms.app.dev
# name: TC-63 Change and restore the User 1 password
# tags: [profile, update-password, security]
# ---
# - runFlow: ../components/open-update-password.yaml
# - inputText: "${USER1_PASSWORD}"
# - inputText: "${TEMPORARY_USER1_PASSWORD}"
# - inputText: "${TEMPORARY_USER1_PASSWORD}"
# - tapOn: "Save Changes"
# The complete implementation must also verify the temporary password and guarantee
# restoration of USER1_PASSWORD before this case can be enabled.
